#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include "globalDef.h"

tokenInfo* createnode(tokenInfo** );
idInfo* createinode(idInfo** );
//header initialise_head();
//iheader initialise_ihead();
